{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="inline-flex items-center px-4 py-2 bg-primary text-white text-sm font-medium rounded-md hover:bg-green-dark transition-colors">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
