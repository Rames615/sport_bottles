# Stateless CSRF only for login/logout (these are token-based actions, not regular forms).
# Regular forms (ShippingAddressType, ContactType, etc.) use session-based CSRF storage by default.
framework:
    csrf_protection:
        stateless_token_ids:
            - authenticate
            - logout
